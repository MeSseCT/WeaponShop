@model IReadOnlyList<Order>

<section class="panel">
    <div class="panel-head">
        <div>
            <h2>Všechny objednávky</h2>
            <p class="panel-subtitle">Kompletní historie všech objednávek v systému.</p>
        </div>
        <div class="btn-row">
            <a class="btn-soft" asp-action="Index">Zpět na schvalování</a>
        </div>
    </div>

    <form method="get" class="form-shell form-shell-wide" style="margin-bottom: 1rem;">
        <div class="grid-two">
            <div class="mb-3">
                <label class="field-label" for="statusFilter">Filtr stavu</label>
                <select id="statusFilter" name="status" class="form-control">
                    <option value="">Vše</option>
                    @foreach (var status in Enum.GetValues<OrderStatus>())
                    {
                        var selected = string.Equals(Context.Request.Query["status"], status.ToString(), StringComparison.OrdinalIgnoreCase);
                        <option value="@status" selected="@(selected ? "selected" : null)">@CatalogPresentation.GetStatusLabel(status)</option>
                    }
                </select>
            </div>
            <div class="mb-3">
                <label class="field-label" for="searchQuery">Hledat (objednávka / e-mail / jméno / výrobní nebo sériové číslo)</label>
                <input id="searchQuery" name="q" class="form-control" value="@Context.Request.Query["q"]" />
            </div>
        </div>
        <button class="btn-modern" type="submit">Použít filtr</button>
    </form>

    @if (TempData["StatusMessage"] is string statusMessage)
    {
        <div class="data-empty">@statusMessage</div>
    }

    @if (TempData["ErrorMessage"] is string errorMessage)
    {
        <div class="data-empty">@errorMessage</div>
    }

    @if (Model.Count == 0)
    {
        <div class="data-empty">Nebyly nalezeny žádné objednávky.</div>
    }
    else
    {
        <div class="table-wrap">
            <table class="data-grid">
                <thead>
                    <tr>
                        <th>Objednávka</th>
                        <th>Stav</th>
                        <th>Zákazník</th>
                        <th>Položky</th>
                        <th class="text-end">Celkem</th>
                        <th class="text-end">Akce</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var order in Model)
                    {
                        <tr>
                            <td>
                                <div>@order.GetPublicOrderNumber()</div>
                                <div class="panel-subtitle">@order.CreatedAt.ToLocalTime().ToString("g")</div>
                            </td>
                            <td><span class="badge-cat">@CatalogPresentation.GetStatusLabel(order.Status)</span></td>
                            <td>
                                <div>@order.User?.FirstName @order.User?.LastName</div>
                                <div class="panel-subtitle">@order.User?.Email</div>
                            </td>
                            <td>@order.Items.Count</td>
                            <td class="text-end">@order.TotalPrice.ToString("C")</td>
                            <td class="text-end">
                                <div class="btn-row">
                                    <a class="btn-inline" asp-area="Admin" asp-action="Details" asp-route-id="@order.Id">Detail</a>
                                    <a class="btn-inline" asp-area="Admin" asp-action="DownloadWord" asp-route-id="@order.Id">Stáhnout RTF</a>
                                    <form asp-area="Admin" asp-action="DeleteHistory" method="post">
                                        <input type="hidden" name="id" value="@order.Id" />
                                        <input type="hidden" name="returnUrl" value="@Context.Request.Path@Context.Request.QueryString" />
                                        <button type="submit" class="btn-inline btn-inline-danger">Smazat</button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</section>
