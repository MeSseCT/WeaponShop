@model IReadOnlyList<Order>

<section class="panel">
    <div class="panel-head">
        <div>
            <h2>Order history</h2>
            <p class="panel-subtitle">All orders for selected customer.</p>
        </div>
        <div class="btn-row">
            <a class="btn-soft" asp-action="Index">Back to approvals</a>
            <a class="btn-soft" asp-action="All">All orders</a>
        </div>
    </div>

    <form method="get" class="form-shell form-shell-wide" style="margin-bottom: 1rem;">
        <input type="hidden" name="userId" value="@Context.Request.Query["userId"]" />
        <div class="mb-3">
            <label class="field-label" for="statusFilter">Status filter</label>
            <select id="statusFilter" name="status" class="form-control">
                <option value="">All</option>
                @foreach (var status in Enum.GetValues<OrderStatus>())
                {
                    var selected = string.Equals(Context.Request.Query["status"], status.ToString(), StringComparison.OrdinalIgnoreCase);
                    <option value="@status" selected="@(selected ? "selected" : null)">@status</option>
                }
            </select>
        </div>
        <button class="btn-modern" type="submit">Apply filter</button>
    </form>

    @if (Model.Count == 0)
    {
        <div class="data-empty">No orders found for this customer.</div>
    }
    else
    {
        <div class="table-wrap">
            <table class="data-grid">
                <thead>
                    <tr>
                        <th>Order</th>
                        <th>Status</th>
                        <th>Customer</th>
                        <th>Items</th>
                        <th>Audit</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var order in Model)
                    {
                        <tr>
                            <td>
                                <div>#@order.Id</div>
                                <div class="panel-subtitle">@order.CreatedAt.ToLocalTime().ToString("g")</div>
                            </td>
                            <td><span class="badge-cat">@order.Status</span></td>
                            <td>
                                <div>@order.User?.FirstName @order.User?.LastName</div>
                                <div class="panel-subtitle">@order.User?.Email</div>
                            </td>
                            <td>
                                @foreach (var item in order.Items)
                                {
                                    <div>@item.Weapon?.Name x @item.Quantity</div>
                                }
                            </td>
                            <td>
                                <div class="audit-list compact">
                                    @if (order.Audits.Count == 0)
                                    {
                                        <div class="panel-subtitle">No audit yet</div>
                                    }
                                    else
                                    {
                                        foreach (var audit in order.Audits.OrderByDescending(a => a.OccurredAtUtc))
                                        {
                                            <div class="audit-chip">
                                                @audit.OccurredAtUtc.ToLocalTime().ToString("g")
                                                • @audit.ActorRole @audit.ActorName
                                                • @audit.Action
                                            </div>
                                        }
                                    }
                                </div>
                            </td>
                            <td class="text-end">
                                <div class="btn-row">
                                    <a class="btn-inline" asp-area="Admin" asp-action="Details" asp-route-id="@order.Id">View</a>
                                    <a class="btn-inline" asp-area="Admin" asp-action="DownloadWord" asp-route-id="@order.Id">Word</a>
                                    <form asp-area="Admin" asp-action="DeleteHistory" method="post">
                                        <input type="hidden" name="id" value="@order.Id" />
                                        <input type="hidden" name="returnUrl" value="@Context.Request.Path@Context.Request.QueryString" />
                                        <button type="submit" class="btn-inline btn-inline-danger">Delete</button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</section>
